# Studio Marketing: decisions taken on Lawrence's behalf

Built 2026-08-26/27 across 14 tasks, 43 commits, merged as d9a2a06.
Every line below is a call I made without asking. Each says what it costs if wrong.

## Rulings

- | T9 | opsActions.insertInternal args (priority,title,rationale,entityType,entityId,payload,dedupeKey) defined in prose only | Ruling: implementer writes it per the prose; same shape as opsBrain inserts, status proposed, autonomy false, source rule, skip on open dedupeKey — cost if wrong: one fix round |
- | T1 self | test imports capabilitiesForTier from ./entitlements; may live in plans.ts | Ruling: import from wherever it is exported; no API change — cost: none |
- | T7/T9 self | tests call t.finishAllScheduledFunctions(() => {}); convex-test needs fake timers | Ruling: use vi.useFakeTimers() + t.finishAllScheduledFunctions(vi.runAllTimers) (or finishInProgressScheduledFunctions) — cost: test-only |
- | T9 self | rate-cut test seeds an empty room and expects recommendations; depends on aiContext.rateCutFor thresholds | Ruling: implementer adjusts the seed to satisfy rateCutFor (documented in plan); the assertion set stays — cost: test-only |
- | T11 self | lucide brand icons (Facebook, Instagram, Linkedin, Youtube) may be deprecated/missing | Ruling: substitute generic lucide icons if an import fails; labels carry the meaning — cost: cosmetic |
- | Global vs repo | "Untitled UI where a new control is needed" vs existing @/components/ui/* | Ruling: existing @/components/ui/* first (repo convention, spec says follow existing patterns); Untitled UI only when nothing fits — cost: none |
- Task 1: implementer DONE_WITH_CONCERNS (commits 1013496, cc3d09e; 1265/1265; ran in LOCAL worktree .claude/worktrees/agent-a04f65403492259ac despite isolation remote). Concern 2 (social_posts not monthly) confirmed real -> pre-review fix dispatched (resume implementer). Ruling: label/enterprise tiers get UNLIMITED social caps (monotonic bundles) - cost if wrong: none.
- Task 1: review clean. Warning resolved: NAV_CAPABILITIES (entitlements.ts:30) gates module toggles + nav locks -> Ruling: Task 11 adds "marketing" to NAV_CAPABILITIES (carry into Task 11 dispatch) - cost if wrong: module toggle would not hide Marketing.
- Task 2: review clean. Warning: Version header 2021-07-28 for Social Planner endpoints unverifiable offline -> Ruling: keep default, Task 14 connector proof validates; per-call override exists - cost if wrong: one-line version change.
- Task 3: review Approved with 4 Important plan-mandated findings. Ruling: fix all four (validate() in createInternal; dup guard on update; redemption-increment test; same-code fall-through test) - they extend the brief, Task 9 calls createInternal - cost if wrong: none. Fix round 1 dispatched (resume implementer), FIX_BASE=ea39574.
- Task 4: review Needs fixes (1 Important, plan-mandated): createBooking src resolution untested. Ruling: fix it, precedent exists in bookingConversion.test.ts for ref - cost if wrong: none.
- Task 5: review Approved. Its lone "Important" (rules for untested platforms) is a FALSE POSITIVE: those branches are verbatim in the brief Step 3 code (plan lines confirmed) -> Ruling: not a defect, no fix round - cost if wrong: none.
- Task 6: review Needs fixes (3 Important, all plan-mandated + REAL): (1) revive branch skips recordUsage(+1) -> cap bypass; (2) myOrgForConnect cap pre-check blocks legit reconnect; (3) choices lacks GHL error guard. Ruling: fix all three in code, spec cap constraint outranks the brief listing - cost if wrong: a studio could exceed its account cap. Fix round 1 dispatched (resumed implementer), FIX_BASE=4c848b1.
- Task 6: parked - listOAuthAccounts returns [] on GHL failure so the choices try/catch is inert; a GHL error reads as "no accounts" in the UI. Ruling: real but not load-bearing here; carry into Task 11 dispatch so the picker says reconnect on an empty list, and flag to final review - cost if wrong: a confusing empty picker.
- Task 6: parked - Finding 2 (fresh connect refused at cap, reconnect exempt) verified by inspection only, untested at the startConnect action layer. Ruling: accept, insertInternal path is tested and is the one that writes - cost if wrong: an inverted !reconnect gate would slip through.
- Task 7: review Needs fixes (1 Important, plan-mandated + REAL): approve double-counts social_posts on retry/edit-reapprove. Ruling: add socialPosts.meteredPeriod stamp, meter once per post per period - cost if wrong: studios blocked early at their cap. Fix round 1 dispatched (resumed implementer), FIX_BASE=c76556c.
- Task 7: fix implementer pushed 7b91ad9 then terminated before appending its fix report; controller session also lost context here. Recovered from git + ledger. Ruling: verify the fix from the pushed commit and a local covering-test run rather than re-dispatching the fix - `npx vitest run convex/marketing/posts.test.ts` 10 passed (was 7); controller wrote the Fix Round 1 section (commit 9d925cf) so the record survives - cost if wrong: the re-review catches it.
- Task 8 pre-flight self-check: the brief's Step 1 test contradicts its own Step 3 code. Test expects postA `redemptions: 2`, but the Step 3 loop counts a redemption only when the visit's own `code` matches the post's promo code: of postA's two attributed bookings, one carries `postId` and no code (not a redemption) and one carries code "A20" (a redemption), so the code produces 1. Ruling: the Step 3 code is right and the test expectation is wrong - a booking attributed by tracked link with no code redeemed nothing, and the spec lists clicks, bookings, revenue and code redemptions as four distinct measures; change the expectation to `redemptions: 1` and leave the implementation as written - cost if wrong: Results under-reports redemptions by the count of code-less bookings, a one-line test change to reverse.
- Task 9 pre-flight defect 1: the brief's test 1 calls `t.finishAllScheduledFunctions(() => {})`, which is inert without fake timers and would leave the post at "approved" instead of "scheduled". Ruling: use the repo convention vi.useFakeTimers() + finishAllScheduledFunctions(vi.runAllTimers), same as the Task 7 precedent - cost if wrong: test-only.
- Task 9 pre-flight defect 2: both brief tests seed orgs with plan:"studio" and no tier, which resolves to tier pro with unlimited caps. Neither test asserts on caps so nothing is currently vacuous. Ruling: set tier:"studio" explicitly anyway for consistency with the Task 7 fixture - cost if wrong: none.
- Task 8 concern 2 (operational, affects every remaining task): a fresh agent worktree has no node_modules, so Node resolution walks up to the MAIN checkout's node_modules and vitest's module glob can silently target the sibling convex/ directory instead of the worktree's. Ruling: keep worktree isolation but make every future implementer dispatch open with two preconditions - symlink node_modules from the main checkout, and verify `git rev-parse HEAD` equals the stated BASE before writing code, re-branching from BASE if not - cost if wrong: an implementer tests the wrong tree and reports green on code it never ran, which is exactly what nearly happened here.
- Task 10 pre-flight defect: the brief's Interfaces line says brandCard.data returns 9 fields including `kind`, but both its Step 2 code and its Step 1 test expectation return 8 and omit `kind`. Ruling: `kind` is a route query-string parameter (?kind=rate_card|open_slot|promo), not part of the Convex query's return; the 8-field shape in the code and the test stands - cost if wrong: the route reads kind from the URL either way, so none.
- Task 8: review Needs fixes (2 Important). Finding 1 is plan-mandated (verbatim brief Step 3 code): perPost scans every bookingVisits row the org ever wrote via by_org_step and filters in JS, though it already takes from/to; the sibling convex/bookingFunnel.ts:150-158 bounds the same table by by_org_day for exactly this reason, and bookingVisits is capped at 5000 rows/day/org. Ruling: fix it - the spec outranks the brief's code, Task 13 builds the Results page on this query, and every visit insert would otherwise invalidate a reactive query that resends the org's whole visit history; bound by by_org_day from dayString(from - WINDOW) to dayString(to + WINDOW), matching the funnel pattern, and keep the existing counting semantics - cost if wrong: clicks recorded more than 7 days before the requested window's start stop counting, which is the intended window anyway.
- Task 8: Finding 2 (no test for the orgsWithAccounts per-org grouping) Ruling: fix it - Grilled.md names cross-org GHL account scoping as a security invariant to be held BY A TEST, and this is the one new fan-out that has none - cost if wrong: none.
- Task 8: warning item resolved by controller - two published posts sharing one promoId make codeToPost keep only the first, so code-only bookings all attribute to the earliest post. Real, and inherent: a booking that carries only a code cannot say which of two posts drove it; the tracked link is what disambiguates. Ruling: not a defect to fix, but require a one-line comment at the codeToPost map so the next reader is not surprised - cost if wrong: a studio double-posting one promo sees the redemptions land on the earlier post.
- Task 11 pre-flight defect 1 (REAL, would not compile): lucide-react in this repo is 1.16.0 and has DROPPED its brand icons. Verified against node_modules/lucide-react/dist/lucide-react.d.ts: Facebook, Instagram, Linkedin and Youtube are all MISSING; Globe, Music2, Pin, AtSign, Cloud and Megaphone are present. The brief's Step 2 import list therefore fails to build as written. Ruling: substitute verified-present non-brand icons, facebook -> Users, instagram -> Camera, linkedin -> Briefcase, youtube -> PlaySquare, and keep Globe/Music2/Music2/Pin/AtSign/Cloud for the rest; the labels carry the meaning - cost if wrong: cosmetic only. This upgrades the original pre-flight's conditional "substitute if an import fails" to a concrete mapping the implementer does not have to discover.
- Task 11 pre-flight defect 2: the brief's Interfaces line types PLATFORM_META values as {label, icon, color} while its Step 2 code writes {label, icon, hint} and every value supplies a hint string. Ruling: the code wins, the field is `hint` and the Accounts page renders it as the per-network caption - cost if wrong: a renamed field.
- Task 11 pre-flight defect 3 (carries the Task 1 ruling): Task 1 added {key:"marketing", nav:true} to convex/lib/modules.ts:171, so FeatureKey and TOGGLEABLE_FEATURES already include it, but NAV_CAPABILITIES in convex/lib/entitlements.ts still does NOT list "marketing". The brief's Step 1 never mentions it. Ruling: Task 11 must add "marketing" to NAV_CAPABILITIES as well as the two files the brief names - cost if wrong: the module toggle silently fails to hide Marketing and disabledFeatures never applies to it.
- Task 12 pre-flight defect (REAL, security + cost): the brief's `suggestCaption` action gates itself with `await ctx.runQuery(api.marketing.accounts.whoAmI, {}); // auth`, but whoAmI is just `currentActor`, and currentActor (convex/lib/tenant.ts:35-38) returns identity?.name ?? identity?.email ?? "Studio" and NEVER THROWS. So the comment is false: an unauthenticated caller reaches a public action that drives paid OpenAI completions. Ruling: gate suggestCaption on a real capability check using this repo's own action-auth pattern, a small query that calls currentOrgWithCapability(ctx, "marketing.edit") invoked via ctx.runQuery, exactly as accounts.myOrgForConnect (convex/marketing/accounts.ts:43-48) does for startConnect and choices - cost if wrong: anonymous traffic burns the studio's OpenAI budget through an open endpoint.
- Task 14 SPLIT RULING: steps 2, 3 and 5 are not implementable by a subagent and must not be attempted by one. Step 2 sets GHL_SOCIAL_USER_ID and PULSE_PUBLIC_HOST on the PROD Convex deployment and adds socialplanner/* scopes to a live Private Integration; step 3 runs marketing.accounts.startConnect against the real GHL connector; step 5 flips per-org module toggles in production. Those are side effects outside this worktree on shared/production systems, which the process says to stop and ask about. Ruling: Task 14's dispatch covers only step 1 (artists.update accepts okToFeature, plus the toggle in edit-artist-dialog.tsx) and step 4 (suite + typecheck + lint); steps 2, 3 and 5 are handed to Lawrence as a rollout checklist at finish, including recording the connector-proof outcome in Grilled.md open question 1 - cost if wrong: none, the code ships either way and the env work needs his GHL account regardless.
- Task 9 pre-flight defect 3: the brief defines open, for the insertInternal dedupe guard, as status "proposed" or "snoozed". The repo's own guard (convex/opsBrain.ts:503) treats OPEN as proposed, approved, snoozed AND executing. Ruling: use the repo's four-status set - an already-approved-but-unexecuted draft is exactly the row a second sweep must not duplicate, and narrowing it to two would let the weekly rate-cut sweep create a duplicate draft post and promo for the same room and code - cost if wrong: duplicate inbox cards and duplicate promos for one recommendation.
- Task 9: consequence worth knowing, ruled acceptable, no fix. runRateCutForAllOrgs (convex/aiActions.ts) fans out to every active org with no tier filter, so a studio-tier org now accumulates opsActions cards it can never see (inbox is Pro+). Ruling: not a defect - the Draft post and Promo the sweep creates ARE reachable on studio tier through the Marketing composer, which is the documented studio path, and the cards become visible if the org upgrades; auto-created discount codes for every org were already pre-existing behavior via ensureDiscountCode - cost if wrong: unusable rows accumulate in opsActions for studio-tier orgs.
- Task 9 concern 4 - CONTROLLER RULING: posts.approveInternal (convex/marketing/posts.ts:137) is plan-mandated dead code. Verified zero callers anywhere in convex/ or src/ outside its own definition; opsActions.approve is a mutation and calls the shared approvePost directly, and Task 13's composer Approve button calls the public posts.approve. Ruling: remove it in the next fix round - an exported internalMutation that can approve and schedule a post with a caller-supplied orgId and no capability check is a live surface with no reason to exist, and the shared approvePost already serves both real callers - cost if wrong: a later task that wanted it re-adds four lines.
- Task 9: review Needs fixes (1 Important, plan-mandated + REAL). Every draft the sweep creates carries accountIds: [], and Step 4 wires the inbox's pre-existing generic Approve button straight to approvePost with no guard. Approving one there: payloadContext returns accounts: [], so schedule's mismatch guard compares 0 !== 0 and never fires; with no GHL env the post is marked "scheduled" then "published" by the sync cron, a FALSE PUBLISHED RECORD for a post that reached no network; with GHL configured it fails silently, since finalize's social_post branch writes no notifications row. None of the three new tests cover it - test 1 seeds accountIds: [ig], sidestepping the exact shape the sweep produces. Ruling: fix in approvePost, not in the page - the guard belongs on the one shared code path both the inbox and the composer call, and a post with zero accounts is never a legitimate approval - cost if wrong: none, the composer picks accounts before approving anyway.
- Task 10 pre-flight defect 2 (typography): the brief's route passes NO `fonts` to ImageResponse and styles the headline `fontFamily: "sans-serif", fontWeight: 800` at 168px. The repo's own precedent (src/app/opengraph-image.tsx:24-55, 239) loads real TTFs for satori, comments that satori cannot consume woff2, and notes that missing glyphs silently fall back. Without a supplied bold face, satori has no 800 weight to use and the headline renders flat, which is the one element the card exists to show. Ruling: reuse the existing loadGoogleFont helper pattern from opengraph-image.tsx and pass fonts explicitly, keeping its null-on-failure degradation so a font fetch hiccup renders a plain card instead of a 500 - cost if wrong: a few extra lines, matching a precedent already in the repo.
- Task 10 pre-flight defect 3 (robustness): the card renders the studio logo with <img src={d.logoUrl}> where logoUrl is a remote Convex storage URL that satori must fetch at request time. Ruling: the route must degrade to no logo if that fetch fails rather than 500, matching how the opengraph precedent degrades on font-fetch failure - cost if wrong: a studio whose logo blob is briefly unreachable gets a broken card route instead of a logoless card.
- Task 10 out-of-brief change 1 ACCEPTED (vitest.config.ts, resolve.preserveSymlinks: true): this is the root cause of the worktree hazard I have been papering over since Task 8 with symlink instructions. Vite resolved the symlinked node_modules to its real path, so convex-test's internal import.meta.glob anchored to the MAIN checkout's convex/ tree; a new module read as missing and an edited one tested the other tree's stale code silently. Verified node_modules in the main checkout is a real directory, so preserveSymlinks is a no-op there today, and the full suite confirms it. Ruling: keep it - it fixes the trap for every remaining task instead of relying on each dispatch's warning - cost if wrong: if node_modules is later symlinked outside Dropbox per the standing Dropbox gotcha, this setting keeps the glob anchored at pulse/, which is still the desired behavior.
- Task 10 Finding 1 CONFIRMED REAL, cross-org leak on a public unauthenticated route: convex/marketing/posts.ts:38 validateInput takes accountIds, promoId, caption, media, includeBookingLink and scheduledFor - roomId is not even a parameter, so it is never validated, while accountIds throws FOREIGN_ACCOUNT (:44) and promoId is org-checked (:50). convex/marketing/brandCard.ts:17-18 then reads room and promo with ctx.db.get and no org check and returns room.name and the derived hourly rate. Chain: any org creates a post carrying another org's roomId, and the public brand-card route serves that org's room name and rate to anyone holding the post id. Ruling: fix BOTH ends, not just the one the reviewer proposed - add roomId org validation in validateInput because that is the root cause and it protects every other surface a post's roomId reaches, AND keep a defensive org check on room and promo in brandCard because it is a public route and defense in depth is the point there; add a regression test seeding a foreign-org room - cost if wrong: none, both checks are cheap and neither has a legitimate case they block.
- Task 10 Finding 2 CONFIRMED: src/middleware.ts:52 uses "/api/brand-card(.*)" and sits TWO LINES above the file's own comment warning that a bare matcher without the slash also matches sibling routes, with every neighbouring entry written as "/book/(.*)", "/pay/(.*)". No sibling route collides today, so blast radius is zero now, but it silently makes any future /api/brand-card-prefixed route public. Ruling: fix to "/api/brand-card/(.*)" - the route always carries a postId segment so nothing legitimate is lost - cost if wrong: none.
- Task 10: parked, real but not load-bearing - createInternal (convex/marketing/posts.ts:279) still does not call validateInput, so the AI-sweep write path remains structurally unvalidated for roomId and promoId ownership. Ruling: accept - its single caller sources rec.roomId from a by_org query on the same orgId it passes, so it is org-owned in practice, and the read-side org check in brandCard is what actually closes this for that path; flag to the final review as a structural gap rather than a live hole - cost if wrong: a future second caller of createInternal could write a foreign room with nothing stopping it at the write layer.
- Task 11 Finding 1 CONFIRMED: Connect and Remove need marketing.approve (convex/marketing/accounts.ts:46,134) but render unconditionally to anyone holding marketing.read. Six of eight studio roles - engineer, assistant_engineer, artist_relations, producer, intern, accountant - hold read but not approve, so clicking either throws AccessError and errorMessage() faithfully surfaces the internal string "studio_member lacks marketing.approve" to the user on this page's two primary actions. The implementer's self-review claimed no page in this codebase pre-emptively hides actions a role cannot perform; that claim is wrong, and the reviewer cited four counter-examples (studio/page.tsx:66,72,138,174; patch/page.tsx:155; patch/device-list.tsx:54; payroll/page.tsx:145) all using the same broad-read-gates-page, narrow-edit-gates-action split. Ruling: fix - verified the helper exists at src/lib/use-capabilities.ts:30 - cost if wrong: none, and leaving it means Tasks 12 and 13 inherit the same gap on Compose and Calendar, which split read from approve identically.
- Task 11 Finding 2 CONFIRMED: neither marketing/layout.tsx nor accounts/page.tsx wraps content in CapabilityGuard, though the nav entry carries capability: "marketing.read" exactly like payments, reports and settings, which all wrap (verified payments/page.tsx:152 uses <CapabilityGuard cap="invoices.read">). A viewer without the capability gets ShellErrorBoundary's generic "Pulse hit a snag" instead of the friendly denial panel. Narrow today since every studio role holds marketing.read, but it is the precedent Tasks 12 and 13 copy. Ruling: fix - CapabilityGuard exists at src/components/shell/capability-guard.tsx:14 - cost if wrong: none.
- Task 12: CONTROLLER RULING CORRECTS THE REVIEWER'S PROPOSED FIX. The reviewer said to delete setLinkTouched(true) from the reload effect. I verified that alone swaps one bug for another: includeBookingLink is NOT persisted on the post (convex/marketing/posts.ts:73,96 destructure it out and only use it to decide whether to write `link`), so the composer re-derives it from whether post.link exists; with the sticky flag gone, the auto-default effect at :144-146 would immediately override that restored value whenever the mix is not Instagram-only, silently discarding an owner's explicit "no link" choice on every reopen. Ruling: auto-adjust on a CHANGE in allInstagramSelected rather than gating on a sticky touched flag, so a reload restores persisted intent, a later account-mix change re-applies the sensible default, and an explicit toggle survives until the mix changes again; and make finalCaption RETRACT "Link in bio" when the mix is no longer Instagram-only, since append-only is half the bug - cost if wrong: the toggle re-defaults once more than an owner expects after they change the account mix, which is the safer direction of the two failures.
- Task 14 concern 1: it ran a one-time additive `npx convex dev --once` push to the DEV deployment because the deployed functions predated its new okToFeature arg, so the end-to-end proof would otherwise have been theatre. Not authorized in the dispatch. Ruling: accept - it is the dev deployment, the change is additive, I had already pushed functions there myself this session, and it checked the risk before acting; prod was never touched - cost if wrong: none, dev now simply matches the branch.
- Task 14: controller resolved the reviewer's one cannot-verify item. .env.example EXISTS but documents NONE of GHL_API_KEY, GHL_LOCATION_ID, GHL_SOCIAL_USER_ID or PULSE_PUBLIC_HOST. The brief listed that file under Files but tied it to Step 2, which I scoped out to Lawrence, so the implementer was right to skip it. Ruling: not a Task 14 defect; carry it as a deferred item into the final review's single fix wave, since documenting the four vars helps the deploy checklist - cost if wrong: Lawrence sets env vars from the runbook instead of from .env.example.

## Deferred and parked, not fixed

- Task 1: minor (deferred): accessPolicies.test covers 4 of 10 edited roles; label/enterprise UNLIMITED caps unpinned by test.
- Task 2: minor (deferred): report file has an em dash inside backticks; wrappers beyond the 7 brief tests untested; PLATFORMS export beyond brief.
- Task 3: minor (deferred): pct clamp duplicated in resolveCode; no inclusive-boundary test; no internal-space normalization test.
- Task 4: minor (deferred): src resolution block duplicated between bookingFunnel.ts and booking.ts (brief-mandated).
- Task 5: minor (deferred): phone regex lacks word boundaries.
- Task 6: minor (deferred): myOrgForConnect/whoAmI could be internalQuery; ghlFromEnv resolution duplicated 3x; remove() throws plain Error not ConvexError; api.d.ts hand-patch unverified against real codegen.
- Task 6: parked - listOAuthAccounts returns [] on GHL failure so the choices try/catch is inert; a GHL error reads as "no accounts" in the UI. Ruling: real but not load-bearing here; carry into Task 11 dispatch so the picker says reconnect on an empty list, and flag to final review - cost if wrong: a confusing empty picker.
- Task 6: parked - Finding 2 (fresh connect refused at cap, reconnect exempt) verified by inspection only, untested at the startConnect action layer. Ruling: accept, insertInternal path is tested and is the one that writes - cost if wrong: an inverted !reconnect gate would slip through.
- Task 7: minor (deferred): captionOverrides stored but never consumed; markStatus accepts a dead "approved" literal; posts.update untested.
- Task 7: minor (deferred): posts.cancel never clears meteredPeriod or decrements usage, so an approve-then-cancel permanently consumes a monthly slot (pre-existing from the original Task 7 implementation, not touched by the fix).
- Task 8: minor (deferred): per-org GHL context resolution now duplicated between posts.ts:296-298 and results.ts:78-81; sequential awaits resolving promos in a loop; perPost also collects published posts without an index-level date bound (low risk under the 20 posts/month cap).
- Task 9: minor (deferred): inbox PayloadIcon has no social_post case and falls through to the note_only icon; the dedupe test exercises only the "approved" member of the four-status open set; two concurrent sweeps for one org could still create a duplicate promo and post with only the second card suppressed (single weekly cron, theoretical).
- Task 10: minor (deferred): a partial Google Fonts failure leaves a fontFamily referenced with no matching fonts entry (inherited from the pre-existing opengraph-image.tsx pattern, bounded by the outer try/catch); preserveSymlinks is a global resolve option to revisit if workspace topology changes; the plan-mandated commit message is not strictly imperative.
- Task 10: parked, real but not load-bearing - createInternal (convex/marketing/posts.ts:279) still does not call validateInput, so the AI-sweep write path remains structurally unvalidated for roomId and promoId ownership. Ruling: accept - its single caller sources rec.roomId from a by_org query on the same orgId it passes, so it is org-owned in practice, and the read-side org check in brandCard is what actually closes this for that path; flag to the final review as a structural gap rather than a live hole - cost if wrong: a future second caller of createInternal could write a foreign room with nothing stopping it at the write layer.
- Task 11: minor (deferred): Remove uses variant="ghost" where a purpose-built `danger` variant exists; no confirmation before disconnecting an account (inherited from the brief); Connect buttons are not pre-emptively disabled at the 3-account studio cap, so a capped owner completes a full OAuth round-trip before being told.
- Task 12: minor (deferred): MediaPicker reuses PhotoUpload's single-slot preview so the button reads "Replace photo" while actually appending a second item; buildFacts() untested; the Accounts/Promo/Artist/Room sections are inlined rather than extracted, making composer.tsx the diff's largest file at 503 lines; media items do not clear on template change; PhotoUpload has no disabled prop for in-flight saves.
- Task 14: minor (deferred): canFeature is derived from can("artists.edit") without checking `loaded`, so the toggle renders briefly disabled on first paint even for an owner (cosmetic, dialog opens post-hydration); and the name canFeature reads as "can this artist be featured" rather than "can the viewer edit this flag".
- Task 13: minor (deferred): a stale line reference in a post-chip.tsx comment (cites posts.ts:240, actual filter is :264); the Sheet's explanatory line only renders when the viewer has NEITHER marketing.edit NOR marketing.approve, so the four roles holding edit-without-approve see an Edit button with no Approve/Cancel/Retry and no explanation; the promo code field uppercases on input but does not strip internal whitespace the way normalizeCode does; all three tables render null while loading rather than a skeleton.
