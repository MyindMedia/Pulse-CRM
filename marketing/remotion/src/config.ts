// Toggle the spoken-line on-screen captions. With voiceover narrating, these
// duplicated lines read as subtitles; set false for a clean, visual-led cut.
// (The end CTA card keeps the logo + URL regardless — that's the call to
// action, not a caption.)
export const SHOW_CAPTIONS = false;
